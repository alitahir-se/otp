{
  "devDependencies": {
    "@commitlint/cli": "^17.8.1",
    "@commitlint/config-conventional": "^17.8.1",
    "husky": "^8.0.0"
  },
  "scripts": {
    "postinstall": "npx husky install && npx husky add .husky/commit-msg  'npx --no -- commitlint --edit ${1}'"
  }
}
